import 'package:flutter/material.dart';

class Space {
  static Widget x(double width) => SizedBox(
        width: width,
      );
  static Widget y(double height) => SizedBox(
        height: height,
      );
}
